const ship = (() => {

    const ShipConstructor = (health) => {
        this.health = health;
    }
    
    // ShipConstructor.prototype.hit = function() {
    //     console.log(this.health);
    // }

    // const generateShips = () => {
    
    // }
    return ShipConstructor
})();

export default ship